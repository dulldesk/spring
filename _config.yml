title: Motion of a Spring
description: A model of the motion of a spring
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com

#plugins:
#  - jekyll-feed

defaults:
  - scope:
      path: ""
    values:
      layout: default

exclude: ["*-cache/", "Gemfile*", "README.md"]

sass:
  style: compressed
